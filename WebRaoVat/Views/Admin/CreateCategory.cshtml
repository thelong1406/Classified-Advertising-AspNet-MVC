@model WebRaoVat.Models.Category

@{
    ViewBag.Title = "Create Main Category";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="title-page">Create Main Category</h2>
<script src="~/Scripts/GetImageToTagImg.js"></script>

@using (Html.BeginForm("CreateCategory","Admin", FormMethod.Post, new { enctype = "multipart/form-data"}))
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <p class="control-label col-md-10">Main Category's Name</p>
            <div class="col-md-10">
                @Html.EditorFor(model => model.category1, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.category1, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <p class="control-label col-md-12">Main Category Image</p>
            <div class="col-md-10">
                <img src="@Url.Content(Model.image)" height="100" width="100" id="previewImg" />
                <input type="file" name="Upload" id="Upload" onchange="ChangeImage(this, document.getElementById('previewImg'))" />
                <p class="validation">@ViewBag.NullImage</p>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <a href="@Url.Action("ManageCategories","Admin")" class="admin-btn-no">&#8678 Manage page</a>
                <input type="submit" value="Create" class="admin-btn" />
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
